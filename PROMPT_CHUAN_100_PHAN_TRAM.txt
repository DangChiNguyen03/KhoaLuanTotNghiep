========================================
PROMPT TẠO USERS (95 users - GIỮ ADMIN CŨ)
========================================

Tạo file JSON cho MongoDB collection "users" với 95 users (KHÔNG TẠO ADMIN vì đã có sẵn).

SCHEMA CHÍNH XÁC:
- name, email, password (required) - Password hash: "$2a$10$abcdefghijklmnopqrstuvwxyz1234567890ABCDEFG"
- role: 'manager', 'staff', 'customer' (KHÔNG TẠO 'admin')
- phone, address, birthday
- employeeId, department, permissions, hireDate, salary, manager (CHỈ cho manager/staff)
- isActive, lastLogin, loginAttempts, lockUntil, cart, date

PHÂN BỐ: 
- 2 managers (role: 'manager', department: 'sales' hoặc 'kitchen', có employeeId, permissions, hireDate, salary)
- 3 staff (role: 'staff', department: 'sales', 'cashier', 'delivery', có employeeId, hireDate, salary)
- 90 customers (role: 'customer', KHÔNG CÓ: employeeId, department, permissions, hireDate, salary, manager)

LƯU Ý: Email phải KHÁC với admin hiện tại (adminyolobrew@gmail.com)

========================================
PROMPT TẠO ORDERS (200 orders)
========================================

Tạo file JSON cho MongoDB collection "orders" với 200 đơn hàng.

SCHEMA CHÍNH XÁC:
- user (ObjectId), items (array)
- items: product, quantity, price, toppings (array ObjectId), sugarLevel, iceLevel, size
- totalPrice, originalPrice, voucher {code, discountAmount}
- paymentMethod: 'cash', 'bank', 'momo', 'zalopay', 'vnpay'
- status: 'pending', 'confirmed', 'preparing', 'ready', 'completed', 'cancelled'
- paymentStatus: 'pending', 'paid', 'failed', 'refunded'
- payment (ObjectId), createdAt

PHÂN BỐ: 100 completed, 40 confirmed, 30 pending, 20 preparing, 10 cancelled

========================================
PROMPT TẠO PAYMENTS (200 payments)
========================================

Tạo file JSON cho MongoDB collection "payments" với 200 payments.

SCHEMA CHÍNH XÁC:
- order, user, amount, paymentMethod, status
- transactionId, bankInfo, notes, paidAt, processedBy
- gatewayTransactionId, gatewayOrderId, gatewayResponse, signature, gatewayMetadata
- createdAt, updatedAt

PHÂN BỐ: 160 paid, 20 pending, 10 failed, 10 refunded

Cash: transactionId = null, không có gateway fields

========================================
PROMPT TẠO VOUCHERS (20 vouchers)
========================================

Tạo file JSON cho MongoDB collection "vouchers" với 20 vouchers.

SCHEMA CHÍNH XÁC:
- code, description, discountType, discountValue
- applicableCategory, specialDay, applicableSize, fixedPrice
- startTime, endTime, isActive, createdAt

discountType: 'percentage', 'fixed_amount', 'special_day_fixed_price'

========================================
PROMPT TẠO AUDIT LOGS (500 logs)
========================================

Tạo file JSON cho MongoDB collection "auditlogs" với 500 logs.

SCHEMA CHÍNH XÁC:
- user, action, resourceType, resourceId
- details, oldValues, newValues
- ipAddress, userAgent, method, endpoint
- status, errorMessage, timestamp, createdAt, updatedAt

========================================
PROMPT TẠO LOGIN LOGS (300 logs)
========================================

Tạo file JSON cho MongoDB collection "loginlogs" với 300 logs.

SCHEMA CHÍNH XÁC:
- user, username, loginTime, ipAddress, userAgent
- loginStatus, failureReason, sessionId, logoutTime, sessionDuration
- deviceInfo {browser, os, device}
- location {country, city, region}
- isActive, riskLevel, notes, createdAt, updatedAt

PHÂN BỐ: 255 success, 45 failed

========================================
PROMPT TẠO PAYMENT METHODS (3 methods)
========================================

Tạo file JSON cho MongoDB collection "paymentmethods" với 3 methods.

SCHEMA CHÍNH XÁC:
- name, code, description, icon
- config {bankName, accountNumber, accountName, apiKey, secretKey, webhookUrl, fee, feeType}
- isActive, order, createdAt, updatedAt

3 methods: Cash, VNPay, MoMo
